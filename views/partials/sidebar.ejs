<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top fadeInDown">
  <div class="container">
    <a class="navbar-brand" href="/pages">(-_(-_-)_-)</a>
   		<div class="navbar-collapse collapse" id="navbarResponsive">
      		<ul class="navbar-nav ml-auto">
		  	<% if(!currentUser){ %>
        		<li class="nav-item active">
          			<a class="nav-link bg-secondary" href="/login">Login</a>
        		</li>
        		<li class="nav-item">
          			<a class="nav-link" href="/register">Sign Up</a>
        		</li>
		  	<% } else { %>
        		<li class="nav-item">
          			<a class="nav-link" href="#">Hello, <strong><%= currentUser.username %> ! </strong></a>
        		</li>
        		<li class="nav-item">
          			<a class="nav-link bg-secondary" href="/logout">Logout</a>
        		</li>
		  	<% } %>
      		</ul>
    	</div>
  </div>
</nav>

		<div class="container fadeInDown">
			<% if(error && error.length > 0) { %>
				<div class="alert alert-danger m-2" role="alert"> 
					<%= error %> 
				</div>
			<% } %>
			<% if(success && success.length > 0) { %>
			<div class="alert alert-success mt-2" role="alert"> 
				<%= success %> 
			</div>
			<% } %>
		</div>